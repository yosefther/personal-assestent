GATEWAY_ALLOW_ALL_USERS=false
TELEGRAM_ALLOW_ALL_USERS=false
DISCORD_ALLOW_ALL_USERS=false

# Normal DNS connectivity is healthy on this host. The upstream fallback-IP
# transport stalled during initialization, so keep it disabled here.
HERMES_TELEGRAM_DISABLE_FALLBACK_IPS=true
